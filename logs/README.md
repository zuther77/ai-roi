# Host-visible application logs (Day 3).
#
# The playout container mounts this directory at /app/logs and writes
# structured JSON lines to playout.jsonl. Everything here is gitignored
# runtime state — only this README is tracked.
